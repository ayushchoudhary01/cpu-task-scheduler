#include <iostream>

// For now this only proves that the project builds and runs.
// The scheduler itself is added in the next steps.
int main() {
    std::cout << "CPU Task Scheduler\n";
    std::cout << "Build OK - no algorithms wired up yet.\n";
    return 0;
}
