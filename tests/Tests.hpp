#pragma once

// Each group of tests lives in its own file and exposes one entry point.
// main.cpp runs them all and reports the tally.
void runCoreTests();
void runAlgorithmTests();
void runPriorityTests();
void runCliTests();
void runRobustnessTests();
